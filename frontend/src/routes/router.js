import { Routes, Route } from 'react-router-dom'

function Router() {
  return (
    <main className='page-root'>
      <Routes>
        <Route
      </Routes>
    </main>
  )
}

export default Router